import React from "react";
import logo from "../assets/logo.svg";

const Navbar = () => {
  return (
    <nav>
      <div>
        <img src={logo} alt="" />
      </div>
    </nav>
  );
};

export default Navbar;
